# Note: Codex vs Claude Code

This repo still contains `.codex/` from the Codex setup. Claude Code can ignore it.

For Claude Code, the primary instruction file is `CLAUDE.md` (plus concise `MEMORY.md`).
