{
  "buildCommand": "npm run generate -w packages/db && npx prisma db push --schema packages/db/prisma/schema.prisma --skip-generate && npm run build -w apps/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
