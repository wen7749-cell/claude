{
  "buildCommand": "npm run db:generate && npm run db:push -- --skip-generate && npm run build -w apps/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
